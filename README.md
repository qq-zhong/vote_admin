# vote_admin

deployment methods were: 
npm install --save-dev gh-pages
npm run build
npx gh-pages -d dist

changes in package.jon

"scripts": {
  "build": "vite build",
  "deploy": "gh-pages -d dist"
}
